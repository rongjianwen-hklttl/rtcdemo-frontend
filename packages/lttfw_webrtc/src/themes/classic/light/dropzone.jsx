import colors from '../../colors'
import chroma from 'chroma-js'

const dropzone = {
  border: '1px dashed '+colors.black.main,
  hover: {
    color: colors.black.main,
    bgColor: colors.grey.light,
  }
}

export default dropzone
