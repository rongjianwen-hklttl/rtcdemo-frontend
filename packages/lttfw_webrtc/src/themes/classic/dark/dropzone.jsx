import colors from '../../colors'
import chroma from 'chroma-js'

const dropzone = {
  border: '1px dashed '+colors.yellow.main,
  hover: {
    color: colors.black.main,
    bgColor: colors.yellow.main,
  }
}

export default dropzone
